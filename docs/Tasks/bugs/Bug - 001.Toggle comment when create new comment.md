# Preproduce

- Click toggle to expand comments
- Create new comment / or toggle like / save

# Expected

- Comment section should expand after comment created

### Root cause

- Replace post

```ts
// just add the comment into the post comments instead of replace whole post
addComment(event: { postId: string; text: string }): void {
    // // test
    // var postComment = this.postsSubject.value.find((p) => p.id === event.postId);
    // postComment?.comments.push({
    //   id: 'de182dda-b289-4462-94a6-395c28befdbc',
    //   avatar: '/assets/design/1.avatar.webp',
    //   name: 'Lan Nguyen',
    //   username: 'nguyenlan',
    //   time: 'Just now',
    //   text: 'comment 2',
    //   likes: 0,
    // });

    // return;

    this.feedService.addComment(event.postId, { text: event.text }).subscribe({
      next: (post) => this.replacePost(post),
      error: () => {
        this.error = 'Cannot add comment right now.';
      },
    });
  }
```
