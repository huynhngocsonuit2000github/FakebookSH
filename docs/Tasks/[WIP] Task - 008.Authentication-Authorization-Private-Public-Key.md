# Integrate with UI

- [x] Change core to use cookies base for BFF, but JWT for calling downstream system, use Private and public key
- [ ] Cookie stores only session id, Tokens are stored server-side, for example Redis / database
