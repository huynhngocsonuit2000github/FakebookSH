Move these later into building-blocks:

```
Common Result / Error classes
Base Entity
Auditable Entity
Domain Event base class
Exception types
Validation behavior
Global exception middleware
Correlation ID middleware
JWT options/config models
Password hashing abstraction
DateTime provider
Current user abstraction
Pagination models
Repository abstractions if you use them
Shared logging helpers
Shared EF Core helpers
```

dotnet sln add
