# Environment to work

- The docker compose environment is use for Development/Test environment, then the local application when develop the feature, we just need to connect to those service
- Kubernet is use for UAT environment
